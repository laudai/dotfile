# Host-level config and secrets
# Copy to ~/.env and fill in values
# Never commit ~/.env

ANYTYPE_API_KEY=        # Anytype API key (from Anytype desktop → Settings → API)
BRAVE_API_KEY=          # Brave Search API key
PLAYWRIGHT_MCP_EXTENSION=  # Playwright MCP extension token
